# Required only when the configured Mission planner uses an authenticated LLM provider.
OPENAI_API_KEY=

# Prefer HTTPS or an SSH tunnel. Set this to 1 only for an explicitly accepted
# remote plaintext HTTP test.
ROBOGUIDE_ALLOW_INSECURE_LLM_HTTP=0
