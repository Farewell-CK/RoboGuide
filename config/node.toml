schema = "roboguide.node-config/v0.6"
node_id = "generic-node"
server_endpoint = "http://127.0.0.1:50051"
state_directory = "node-state"
reconnect_delay_ms = 1000

[artifacts]
endpoint = "http://127.0.0.1:8090"
cache_directory = "artifact-cache"
max_artifact_bytes = 4294967296
chunk_size_bytes = 1048576
connect_timeout_ms = 5000
read_timeout_ms = 30000

[[local_systems]]
id = "local-runtime"
runtime_name = "local-eaios"
runtime_version = "0.1.0"

[local_systems.health]
state_pointer = "/state"
detail_pointer = "/detail"
online = ["ONLINE"]
degraded = ["DEGRADED"]
offline = ["OFFLINE"]

[local_systems.health.step]
id = "runtime-health"
connection = "local-http"

[local_systems.health.step.operation]
kind = "http"
method = "GET"
path = "/v1/health"

[local_systems.health.step.request]
base = {}

[[local_systems]]
id = "local-sensors"
runtime_name = "sensor-runtime"
runtime_version = "1.0.0"

[local_systems.health]
state_pointer = "/state"
detail_pointer = "/detail"
online = ["ONLINE"]
degraded = ["DEGRADED"]
offline = ["OFFLINE"]

[local_systems.health.step]
id = "sensor-health"
connection = "sensor-http"

[local_systems.health.step.operation]
kind = "http"
method = "GET"
path = "/v1/health"

[local_systems.health.step.request]
base = {}

[[connections]]
driver = "http"
id = "local-http"
local_system = "local-runtime"
endpoint = "http://127.0.0.1:8081"
timeout_ms = 5000

[[connections]]
driver = "http"
id = "sensor-http"
local_system = "local-sensors"
endpoint = "http://127.0.0.1:8082"
timeout_ms = 5000

[[capabilities]]
contract = "compute.noop@v1"
kind = "compute"
owner = "local-runtime"
required_resources = ["compute-slot"]
local_locks = ["noop-executor"]

[[resources]]
id = "compute-slot"
kind = "compute"
capacity = 1
owner = "local-runtime"

[[sensors]]
id = "front-camera"
kind = "rgb-camera"
owner = "local-sensors"

[[state_exports]]
id = "runtime-summary"
owner = "local-runtime"
object_class = "node"
object_type = "runtime"
object_id = "generic-node"
semantic = "reported"
payload_schema = "roboguide.example.runtime-health/v0.1"
valid_for_ms = 15000
interval_ms = 5000
value_pointer = ""

[state_exports.step]
id = "runtime-state"
connection = "local-http"

[state_exports.step.operation]
kind = "http"
method = "GET"
path = "/v1/health"

[state_exports.step.request]
base = {}

[[memory_providers]]
id = "local-artifacts"
owner = "local-runtime"
kind = "artifact"
scope = "local"
visibility = "discoverable"
payload_schema = "roboguide.example.local-artifact/v0.1"
media_type = "application/octet-stream"

[capabilities.workflow]
poll_interval_ms = 250

[capabilities.readiness]
state_pointer = "/state"
detail_pointer = "/detail"
ready = ["READY"]
unavailable = ["UNAVAILABLE"]

[capabilities.readiness.step]
id = "compute-readiness"
connection = "local-http"

[capabilities.readiness.step.operation]
kind = "http"
method = "GET"
path = "/v1/capabilities/compute.noop"

[capabilities.readiness.step.request]
base = {}

[capabilities.workflow.local_handle]
kind = "pointer"
pointer = "/steps/dispatch/execution_id"

[capabilities.workflow.execution_state]
state_pointer = "/steps/status/state"
reason_pointer = "/steps/status/detail"
accepted = ["ACCEPTED"]
running = ["RUNNING"]
completed = ["COMPLETED"]
failed = ["FAILED"]
cancelled = ["CANCELLED"]
case_sensitive = false

[[capabilities.workflow.execute]]
id = "dispatch"
connection = "local-http"

[capabilities.workflow.execute.operation]
kind = "http"
method = "POST"
path = "/v1/executions"

[capabilities.workflow.execute.request]
base = {}

[[capabilities.workflow.execute.request.bindings]]
target = "/invocation"

[capabilities.workflow.execute.request.bindings.value]
kind = "pointer"
pointer = "/invocation"

[[capabilities.workflow.status]]
id = "status"
connection = "local-http"

[capabilities.workflow.status.operation]
kind = "http"
method = "POST"
path = "/v1/executions/status"

[capabilities.workflow.status.request]
base = {}

[[capabilities.workflow.status.request.bindings]]
target = "/execution_id"

[capabilities.workflow.status.request.bindings.value]
kind = "pointer"
pointer = "/local_handle"

[[capabilities.workflow.cancel]]
id = "cancel"
connection = "local-http"

[capabilities.workflow.cancel.operation]
kind = "http"
method = "POST"
path = "/v1/executions/cancel"

[capabilities.workflow.cancel.request]
base = {}

[[capabilities.workflow.cancel.request.bindings]]
target = "/execution_id"

[capabilities.workflow.cancel.request.bindings.value]
kind = "pointer"
pointer = "/local_handle"
